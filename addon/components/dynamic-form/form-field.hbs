{{#if this.formElement}}
  {{#let this.formElement as |formElement|}}
    <formElement
      @data={{@data}}
      @dataSchema={{@dataSchema}}
      @formId={{@formId}}
      @elementOpts={{@elementOpts}}
      ...attributes/>
  {{/let}}
{{/if}}