{{#if this.formField}}
  {{#let this.formField as |formField|}}
    <formField
      @data={{@data}}
      @dataSchema={{@dataSchema}}
      @formId={{@formId}}
      @elementOpts={{@elementOpts}}
      ...attributes/>
  {{/let}}
{{/if}}